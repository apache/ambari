import { PipeTransform } from '@angular/core';
export declare type ByteUnit = 'B' | 'KB' | 'MB' | 'GB' | 'TB';
export declare class BytesPipe implements PipeTransform {
    static formats: {
        [key: string]: {
            max: number;
            prev?: ByteUnit;
        };
    };
    transform(input: any, decimal?: number, from?: ByteUnit): any;
}
