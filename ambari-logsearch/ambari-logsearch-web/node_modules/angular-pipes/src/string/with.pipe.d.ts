import { PipeTransform } from '@angular/core';
export declare class WithPipe implements PipeTransform {
    transform(input: string, start?: string | null, ends?: string | null, csensitive?: boolean): any;
}
