"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var core_1 = require("@angular/core");
var utils_1 = require("../utils/utils");
var RandomPipe = (function () {
    function RandomPipe() {
    }
    RandomPipe.prototype.transform = function (input, min, max) {
        if (min === void 0) { min = 0; }
        if (max === void 0) { max = 1; }
        if (!utils_1.isNumberFinite(min) || !utils_1.isNumberFinite(max)) {
            return input;
        }
        if (min > max) {
            max = min;
            min = 0;
        }
        return Math.random() * (max - min) + min;
    };
    return RandomPipe;
}());
RandomPipe.decorators = [
    { type: core_1.Pipe, args: [{
                name: 'random'
            },] },
];
/** @nocollapse */
RandomPipe.ctorParameters = function () { return []; };
exports.RandomPipe = RandomPipe;
//# sourceMappingURL=random.pipe.js.map