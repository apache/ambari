"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var core_1 = require("@angular/core");
var utils_1 = require("../utils/utils");
var DefaultsPipe = (function () {
    function DefaultsPipe() {
    }
    DefaultsPipe.prototype.transform = function (input, defaults) {
        if (!utils_1.isObject(defaults)) {
            return input;
        }
        if (utils_1.isNil(input)) {
            return defaults;
        }
        if (utils_1.isArray(input)) {
            return input.map(function (item) {
                if (utils_1.isObject(item)) {
                    return Object.assign({}, defaults, item);
                }
                if (utils_1.isNil(item)) {
                    return defaults;
                }
                return item;
            });
        }
        if (utils_1.isObject(input)) {
            return Object.assign({}, defaults, input);
        }
        return input;
    };
    return DefaultsPipe;
}());
DefaultsPipe.decorators = [
    { type: core_1.Pipe, args: [{ name: 'defaults' },] },
];
/** @nocollapse */
DefaultsPipe.ctorParameters = function () { return []; };
exports.DefaultsPipe = DefaultsPipe;
//# sourceMappingURL=defaults.pipe.js.map